*.jar
logs/*

# Runtime files (JAR, config, logs, uploads) - keep folder structure
runtime/*
!runtime/.gitkeep
!runtime/logs/.gitkeep
!runtime/uploads/.gitkeep

# Docker environment files
.env
.env.local
.env.*.local

# Docker volumes and data
backups/
uploads/
